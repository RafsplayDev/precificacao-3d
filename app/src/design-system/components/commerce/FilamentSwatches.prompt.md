Round filament swatches — how customers choose the colour of a printed product.

```jsx
<FilamentSwatches value={cor} onChange={setCor} showName />
```

Import `FILAMENTS` for the canonical eight-colour palette. Selected state = 2px ink ring, never a checkmark.
