The single container primitive: 18px radius, 1px hairline border, white surface.

```jsx
<Card elevation="raised" interactive padding="var(--space-5)">…</Card>
```

Default is flat + hairline. Reserve `raised` for floating panels; never stack a shadow inside another shadowed card.
