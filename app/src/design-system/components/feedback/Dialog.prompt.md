Confirmations and short flows (add-to-cart, cancel plan, license terms).

```jsx
<Dialog title="Adicionar ao carrinho?" description="Escolha a cor do filamento antes de fechar."
  onClose={close} footer={<><Button variant="ghost" onClick={close}>Voltar</Button><Button variant="accent">Adicionar</Button></>} />
```
