Switches between sibling views or filters.

```jsx
<Tabs variant="underline" value={tab} onChange={setTab}
  items={[{id:"todos",label:"Todos",count:48},{id:"novos",label:"Novos",count:6}]} />
```

`pill` for catalogue filters, `underline` for in-page sections.
