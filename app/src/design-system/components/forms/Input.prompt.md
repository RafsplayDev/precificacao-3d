Text field — 8px radius (inputs are the one place Drop Color does not use pills).

```jsx
<Input label="E-mail" placeholder="voce@email.com" hint="Usamos só para o rastreio." />
<Input label="Altura" suffix="mm" defaultValue="120" />
```
